language: 'javascript'

dependsOn: ['common', 'server']

tags: ['frontend']

tasks:
  build:
    command: 'build'
    deps: ['^:build']
  lint:
    command: 'lint'
    tags: ['quality']
  test:
    command: 'test'
    tags: ['quality']
