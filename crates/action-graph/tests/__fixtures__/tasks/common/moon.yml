dependsOn: ['base']

tags: ['frontend']

tasks:
  build:
    command: 'build'
  lint:
    command: 'lint'
    tags: ['quality']
  internal:
    command: 'unknown'
    options:
      internal: true
    tags: ['quality']
