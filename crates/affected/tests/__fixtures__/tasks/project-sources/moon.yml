dependsOn:
  - 'dep'
  - 'dep-test'

tasks:
  by-any:
    inputs:
      - 'project://dep'
  by-deps:
    inputs:
      - project: '^'
        group: 'tests'
  by-group:
    inputs:
      - project: 'dep'
        group: 'sources'
  by-filter:
    inputs:
      - 'project://dep?filter=tests/**/*'
