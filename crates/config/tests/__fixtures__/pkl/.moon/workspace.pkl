codeowners {
	globalPaths = new Mapping {
		["*"] = List("@admins")
	}
	// orderBy = "project-name"
	requiredApprovals = 1
	syncOnRun = true
}

constraints {
	enforceProjectTypeRelationships = true
	tagRelationships = new Mapping {
		["a"] = List("b", "c")
	}
}
