[package]
name = "moon_platform_runtime"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
moon_config = { path = "../../../nextgen/config" }
semver = { workspace = true }
serde = { workspace = true }
