[package]
name = "moon_node"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
moon_cli = { path = "../cli" }
napi = { version = "2.1", default-features = false, features = ["napi4"] }
napi-derive = "2.1"

[build-dependencies]
napi-build = "1.2"
