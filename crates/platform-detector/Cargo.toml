[package]
name = "moon_platform_detector"
version = "0.0.1"
edition = "2021"
publish = false

[dependencies]
moon_config = { path = "../config" }
once_cell = { workspace = true }
regex = { workspace = true }

[lints]
workspace = true
