inheritedBy:
  toolchains: 'node'

env:
  SHARED: 'node'

fileGroups:
  sources:
    - 'node'

tasks:
  global-system:
    command: 'node-global-override'
    inputs:
      - 'in-override'
    outputs:
      - 'out-override'
  node-standard:
    command: 'node-standard'
