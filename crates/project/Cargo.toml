[package]
name = "monolith_project"
version = "0.1.0"
edition = "2021"

[dependencies]
monolith_config = { path = "../config" }
monolith_logger = { path = "../logger" }
monolith_task = { path = "../task" }
itertools = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
solvent = "0.8"
thiserror = "1.0"
