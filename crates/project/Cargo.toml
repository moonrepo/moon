[package]
name = "moon_project"
version = "0.1.1"
edition = "2024"
license = "MIT"
description = "A project in moon."
homepage = "https://moonrepo.dev/moon"
repository = "https://github.com/moonrepo/moon"
publish = true

[dependencies]
moon_common = { version = "0.1.0", path = "../common" }
moon_config = { version = "0.1.2", path = "../config" }
moon_file_group = { version = "0.1.0", path = "../file-group" }
moon_task = { version = "0.1.0", path = "../task" }
miette = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
