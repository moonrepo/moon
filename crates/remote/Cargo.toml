[package]
name = "moon_remote"
version = "0.0.1"
edition = "2021"
publish = false

[dependencies]
moon_config = { path = "../config" }
moon_project = { path = "../project" }
moon_task = { path = "../task" }
bazel-remote-apis = "0.6.0"
miette = { workspace = true }
starbase_utils = { workspace = true }
tokio = { workspace = true }
tonic = { version = "0.12.2", default-features = false, features = [
	"channel",
	"gzip",
	"tls-native-roots",
] }

[lints]
workspace = true
