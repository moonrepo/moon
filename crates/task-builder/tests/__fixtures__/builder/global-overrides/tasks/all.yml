tasks:
  deploy:
    command: 'deploy'
    deps:
      - 'test'
      - '~:build'
      - '^:test'
      - '^:build'
      - '~:#tagged'
      - 'override-global:#tagged'
  build:
    command: 'build-global'
  test:
    command: 'test-global'
