[package]
name = "monolith_workspace"
version = "0.1.0"
edition = "2021"

[dependencies]
figment = { version = "0.10", features = ["yaml"] }
thiserror = "1.0.30"
serde = { version = "1.0", features = ["derive"] }
