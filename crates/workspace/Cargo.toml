[package]
name = "monolith_workspace"
version = "0.1.0"
edition = "2021"

[dependencies]
monolith_config = { path = "../config" }
monolith_project = { path = "../project" }
monolith_toolchain = { path = "../toolchain" }
thiserror = "1.0"
