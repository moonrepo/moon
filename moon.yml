id: 'root'

tasks:
  check:
    command: 'vp check'
    inputs: []
    options:
      cache: false

  lint:
    command: 'vp lint --fix'
    preset: 'utility'

  format:
    command: 'vp fmt --write'
    preset: 'utility'

workspace:
  inheritedTasks:
    exclude: ['build', 'test', 'typecheck']
