tasks:
  base:
    command: noop

  outputs:
    command: noop
    outputs:
      - 'file.txt'

  has-deps:
    command: noop
    deps:
      - 'base'

  no-deps:
    command: noop

  no-cache:
    command: noop
    options:
      cache: false
