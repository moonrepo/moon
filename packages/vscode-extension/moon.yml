language: typescript
stack: frontend
layer: tool
tags:
  - extension

project:
  title: VS Code extension
  description: This is to purely test metadata in the extension.

workspace:
  inheritedTasks:
    exclude:
      - build
      - test

tasks:
  build:
    command: yarn run compile
    inputs:
      - src/**/*
    outputs:
      - lib
