tags: [standard]

tasks:
  standard:
    command: noop
  runFromProject:
    command: echo 'from project'
    toolchain: system
  runFromWorkspace:
    command: echo 'from workspace'
    toolchain: system
    options:
      runFromWorkspaceRoot: true
  localOnly:
    command: node --version
    toolchain: node
    local: true
  internalOnly:
    command: node --version
    toolchain: node
    options:
      internal: true
