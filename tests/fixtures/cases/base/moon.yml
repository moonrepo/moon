tasks:
  base:
    command: noop
  runFromProject:
    command: noop
    platform: system
  runFromWorkspace:
    command: noop
    platform: system
    options:
      runFromWorkspaceRoot: true
  localOnly:
    command: noop
    platform: system
    local: true
