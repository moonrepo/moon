tags: [standard]

tasks:
  standard:
    command: noop
  runFromProject:
    command: echo 'from project'
    toolchain: system
  runFromWorkspace:
    command: echo 'from workspace'
    toolchain: system
    options:
      runFromWorkspaceRoot: true
  internalOnly:
    command: node --version
    toolchain: node
    options:
      internal: true
