tasks:
  base:
    command: noop
  runFromProject:
    command: node --version
    platform: node
  runFromWorkspace:
    command: node --version
    platform: node
    options:
      runFromWorkspaceRoot: true
  localOnly:
    command: node --version
    platform: node
    local: true
