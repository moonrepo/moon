tasks:
  base:
    command: noop
  runFromProject:
    command: echo
    args: '"in project"'
    platform: system
  runFromWorkspace:
    command: echo
    args: '"in workspace"'
    options:
      runFromWorkspaceRoot: true
    platform: system
  localOnly:
    command: 'echo "local only"'
    platform: system
    local: true
