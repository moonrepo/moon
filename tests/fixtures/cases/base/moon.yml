tasks:
  base:
    command: noop
  runFromProject:
    command: echo
    args: '"in project"'
    type: system
  runFromWorkspace:
    command: echo
    args: '"in workspace"'
    options:
      runFromWorkspaceRoot: true
    type: system
  localOnly:
    command: 'echo "local only"'
    type: system
    local: true
