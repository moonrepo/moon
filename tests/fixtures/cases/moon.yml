tasks:
  noop:
    command: noop
    inputs:
      - '*.json'
  oneOff:
    command: node
    args: -e "console.log('root one off')"
    inputs: []
    options:
      cache: false

workspace:
  inheritedTasks:
    include: []
