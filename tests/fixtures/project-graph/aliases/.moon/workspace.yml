node:
  # Use a unique version as to not collide with other tests
  version: '16.0.0'
  aliasPackageNames: 'name-and-scope'

projects:
  noLang: no-lang
  nodeNameOnly: node-name-only
  nodeNameScoped: node-name-scope
