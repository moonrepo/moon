extends: '../shared-workspace.yml'

projects:
  advanced: advanced
  basic: basic
  emptyConfig: empty-config
  noConfig: no-config

  # Deps
  foo: deps/foo
  bar: deps/bar
  baz: deps/baz

  # Tasks
  tasks: tasks

  # Languages + types
  js: langs/js
  ts: langs/ts
  bash: langs/bash
