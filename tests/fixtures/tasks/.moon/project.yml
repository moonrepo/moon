tasks:
  standard:
    command: cmd
  withArgs:
    command: cmd
    args:
      - --foo
      - --bar
      - baz
  withInputs:
    command: cmd
    inputs:
      - rel/file.*
      - /root.*
  withOutputs:
    command: cmd
    outputs:
      - lib
      - /build
