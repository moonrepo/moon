env:
  PROJECT: 'true'
  SOURCE: project-level
  SUB: '${VALID}'

tasks:
  inherit: {}
  env:
    env:
      SOURCE: task-level
      TASK: 'true'
  envFile:
    options:
      envFile: true
  all:
    env:
      SOURCE: task-level
      TASK: 'true'
    options:
      envFile: true
