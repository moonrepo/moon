tasks:
  standard:
    deps:
      - b:standard
    args:
      - --b
    inputs:
      - b.*
    outputs:
      - b.ts
    options:
      mergeArgs: append
      mergeDeps: prepend
      mergeInputs: replace
      mergeOutputs: append
