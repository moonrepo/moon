tasks:
  standard:
    type: shell
    deps:
      - b:standard
    args:
      - --b
    inputs:
      - b.*
    outputs:
      - b.ts
    options:
      mergeArgs: append
      mergeDeps: append
      mergeInputs: append
      mergeOutputs: append
