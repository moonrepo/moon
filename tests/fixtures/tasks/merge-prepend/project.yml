tasks:
  standard:
    type: shell
    command: newcmd
    deps:
      - b:standard
    args:
      - --b
    inputs:
      - b.*
    outputs:
      - b.ts
    options:
      mergeArgs: prepend
      mergeDeps: prepend
      mergeInputs: prepend
      mergeOutputs: prepend
