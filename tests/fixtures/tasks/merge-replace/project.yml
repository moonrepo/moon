tasks:
  standard:
    type: shell
    command: newcmd
    dependsOn:
      - b:standard
    args:
      - --b
    inputs:
      - b.*
    outputs:
      - b
    options:
      mergeStrategy: replace
