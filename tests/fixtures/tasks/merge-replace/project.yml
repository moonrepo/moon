tasks:
  standard:
    type: shell
    command: newcmd
    deps:
      - b:standard
    args:
      - --b
    inputs:
      - b.*
    outputs:
      - b.ts
    options:
      mergeArgs: replace
      mergeDeps: replace
      mergeInputs: replace
      mergeOutputs: replace
