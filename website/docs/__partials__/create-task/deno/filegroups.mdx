```yaml title="<project>/moon.yml" {8,10}
language: 'typescript'

fileGroups:
	# ...

tasks:
	build:
		command: 'deno compile ./src/main.ts -o ./out.js'
		inputs:
			- '@globs(sources)'
			- 'deno.*'
		outputs:
			- 'out.js'
```
