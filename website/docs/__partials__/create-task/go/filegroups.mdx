```yaml title="<project>/moon.yml" {8,10}
language: 'go'

fileGroups:
  # ...

tasks:
  build:
    command: 'go build -o @out(0)'
    inputs:
      - '@globs(sources)'
    outputs:
      - 'build' # Just an example!
```
