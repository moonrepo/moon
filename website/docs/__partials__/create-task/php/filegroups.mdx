```yaml title="<project>/moon.yml" {10}
language: 'php'

fileGroups:
	# ...

tasks:
	build:
		command: 'phar pack -h sha256'
		inputs:
			- '@globs(sources)'
			- 'composer.json'
			- '/composer.json'
		outputs:
			- 'file.phar' # Just an example!
```
