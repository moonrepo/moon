```yaml title="<project>/moon.yml" {10,11}
language: 'php'

tasks:
	build:
		command: 'phar pack -h sha256'
		inputs:
			- 'src/**/*'
			- 'composer.json'
			- '/composer.json'
		outputs:
			- 'file.phar' # Just an example!
```
