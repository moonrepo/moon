```yaml title="<project>/moon.yml" {10}
language: 'python'

inputGroups:
  # ...

tasks:
  build:
    command: 'python -m build'
    inputs:
      - '@globs(sources)'
      - 'pyproject.toml'
      - '/.python-version'
    outputs:
      - 'dist'
```
