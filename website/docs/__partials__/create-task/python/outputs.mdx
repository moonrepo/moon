```yaml title="<project>/moon.yml" {10,11}
language: 'python'

tasks:
	build:
		command: 'python -m build'
		inputs:
			- 'src/**/*'
			- 'pyproject.toml'
			- '/.python-version'
		outputs:
			- 'dist'
```
