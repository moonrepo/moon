```yaml title="<project>/moon.yml" {4,5}
language: 'ruby'

tasks:
	build:
		command: 'rake build'
```

:::caution

Ruby doesn't have a concept of "building" applications or libraries, so this example and the the
following examples are merely theoretical.

:::
