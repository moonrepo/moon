```yaml title="<project>/moon.yml" {10}
language: 'ruby'

inputGroups:
  # ...

tasks:
  build:
    command: 'rake build --quiet'
    inputs:
      - '@globs(sources)'
      - 'Rakefile'
      - '/Gemfile'
    outputs:
      - 'build' # Just an example!
```
