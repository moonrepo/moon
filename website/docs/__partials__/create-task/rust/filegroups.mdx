```yaml title="<project>/moon.yml" {10}
language: 'rust'

inputGroups:
  # ...

tasks:
  build:
    command: 'cargo build --release'
    inputs:
      - '@globs(sources)'
      - 'Cargo.toml'
      - '/Cargo.toml'
    outputs:
      - 'target/release' # Just an example!
```
