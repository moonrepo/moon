---
title: use
---

The `proto use` (or `proto u`) command will download and install _all_ tools _and_ plugins from the
closest [`.prototools`](../config) configuration file.

```shell
$ proto use
```
