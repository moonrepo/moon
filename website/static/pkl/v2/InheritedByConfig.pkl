// Automatically generated by schematic. DO NOT MODIFY!

/// Configures conditions that must match against a project for tasks
/// to be inherited. If multiple conditions are defined, then all must match
/// for inheritance to occur. If no conditions are defined, then tasks will
/// be inherited by all projects.
module InheritedByConfig

import "InheritedConditionConfig.pkl"
import "LanguageType.pkl"
import "LayerType.pkl"
import "StackType.pkl"

/// Condition that matches against literal files within a project.
/// If multiple values are provided, at least 1 file needs to exist.
files: (String | Listing<String> | List<String> | Set<String>)? = file

/// An alias of `files`.
hidden file: (String | Listing<String> | List<String> | Set<String>)?

/// Condition that matches against a project's `language`.
/// If multiple values are provided, it matches using an OR operator.
languages: (LanguageType.LanguageType | Listing<LanguageType.LanguageType> | List<LanguageType.LanguageType> | Set<LanguageType.LanguageType>)? = language

/// An alias of `languages`.
hidden language: (LanguageType.LanguageType | Listing<LanguageType.LanguageType> | List<LanguageType.LanguageType> | Set<LanguageType.LanguageType>)?

/// Condition that matches against a project's `layer`.
/// If multiple values are provided, it matches using an OR operator.
layers: (LayerType.LayerType | Listing<LayerType.LayerType> | List<LayerType.LayerType> | Set<LayerType.LayerType>)? = layer

/// An alias of `layers`.
hidden layer: (LayerType.LayerType | Listing<LayerType.LayerType> | List<LayerType.LayerType> | Set<LayerType.LayerType>)?

/// The order in which this configuration is inherited by a project.
/// Lower is inherited first, while higher is last.
order: UInt16?

/// Condition that matches against a project's `stack`.
/// If multiple values are provided, it matches using an OR operator.
stacks: (StackType.StackType | Listing<StackType.StackType> | List<StackType.StackType> | Set<StackType.StackType>)? = stack

/// An alias of `stacks`.
hidden stack: (StackType.StackType | Listing<StackType.StackType> | List<StackType.StackType> | Set<StackType.StackType>)?

/// Condition that matches against a tag within the project.
tags: InheritedConditionConfig.InheritedConditionConfig? = tag

/// An alias of `tags`.
hidden tag: InheritedConditionConfig.InheritedConditionConfig?

/// Condition that matches against a toolchain detected for a project.
toolchains: InheritedConditionConfig.InheritedConditionConfig? = toolchain

/// An alias of `toolchains`.
hidden toolchain: InheritedConditionConfig.InheritedConditionConfig?
